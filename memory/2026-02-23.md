# 2026-02-23 - Nebula Chat P2P 聊天软件开发

## 项目信息
- **项目名称**: Nebula Chat
- **项目位置**: `F:\nebula-chat\`
- **目标**: 去中心化 P2P 加密聊天软件（类似 Telegram/微信）

---

## 今日进度 (Day 1 + Day 2)

### ✅ 已完成

#### 环境搭建
- [x] 安装 Rust (rustup)
- [x] 安装 Visual Studio Build Tools 2022
- [x] 安装 Node.js 依赖

#### 项目结构
- [x] Tauri v2 + React 18 + TypeScript 项目
- [x] TailwindCSS 样式系统
- [x] SQLite 数据库集成
- [x] libsodium 加密库集成

#### 后端实现 (Rust)
- [x] 密钥生成 (Ed25519) - `crypto::generate_identity()`
- [x] 身份导出/导入 - `crypto::export_identity()`, `crypto::import_contact()`
- [x] SQLite 数据库初始化 - `db::init_db()`
- [x] 联系人管理 - `db::add_contact()`, `db::list_contacts()`, `db::remove_contact()`
- [x] 消息存储 - `db::add_message()`, `db::get_messages()`
- [x] Tauri Commands 全部实现

#### 前端实现 (React)
- [x] 主界面布局 (ChatLayout)
- [x] Onboarding 引导界面
- [x] 消息气泡组件
- [x] 联系人列表组件
- [x] 深色主题样式

---

## ⚠️ 已知问题

### 🔴 窗口闪退问题 (未解决)
**现象**: 应用启动后窗口会显示，但几秒后自动关闭/消失

**可能原因**:
1. React 组件渲染错误导致崩溃
2. Tauri Webview2 初始化失败
3. 前端调用后端 Command 时类型不匹配
4. CSS 样式兼容性问题

**调试信息**:
- 进程正常启动 (`nebula-chat.exe`)
- Vite 开发服务器正常运行 (`http://localhost:5173`)
- 数据库文件正常创建 (`%APPDATA%\com.nebula.chat\nebula\messages.db`)
- 编译无错误，只有警告 (10 个未使用变量警告)

**待办**: 
- 检查浏览器控制台错误日志
- 简化前端代码排查问题
- 添加错误边界组件

---

## 📁 项目文件结构

```
F:\nebula-chat\
├── src/                          # React 前端
│   ├── App.tsx                   # 主组件
│   ├── main.tsx                  # 入口
│   ├── index.css                 # 全局样式
│   ├── components/
│   │   ├── ChatLayout.tsx        # 聊天界面
│   │   └── Onboarding.tsx        # 引导界面
│   └── lib/
│       └── utils.ts              # 工具函数
├── src-tauri/                    # Rust 后端
│   ├── src/
│   │   ├── main.rs               # 入口
│   │   ├── commands/             # Tauri Commands
│   │   │   ├── mod.rs
│   │   │   ├── chat.rs           # 消息相关
│   │   │   ├── contacts.rs       # 联系人管理
│   │   │   ├── crypto.rs         # 密钥管理
│   │   │   └── config.rs         # 设置
│   │   ├── crypto/
│   │   │   └── mod.rs            # 加密实现
│   │   ├── db/
│   │   │   └── mod.rs            # 数据库实现
│   │   ├── p2p/
│   │   │   └── mod.rs            # P2P 引擎 (占位符)
│   │   └── config/
│   │       └── mod.rs            # 配置管理
│   ├── Cargo.toml                # Rust 依赖
│   ├── tauri.conf.json           # Tauri 配置
│   ├── build.rs                  # 构建脚本
│   ├── icons/                    # 应用图标
│   └── build.bat                 # 编译脚本
├── dist/                         # 前端构建输出
├── package.json                  # 前端依赖
├── vite.config.ts                # Vite 配置
├── tailwind.config.js            # Tailwind 配置
└── tsconfig.json                 # TypeScript 配置
```

---

## 🔧 技术栈

| 组件 | 技术 |
|------|------|
| 桌面框架 | Tauri v2 |
| 前端 | React 18 + TypeScript |
| UI | TailwindCSS + shadcn/ui 风格 |
| P2P | libp2p + WebRTC (待实现) |
| 加密 | libsodium (Ed25519, X25519) |
| 数据库 | SQLite (rusqlite) |
| 构建 | Vite + Cargo |

---

## 📋 下一步计划

### Day 3: 修复 + P2P 通信
1. **修复窗口闪退问题** (优先级 🔴)
2. 实现 libp2p 集成
3. 实现 WebRTC 直连
4. 实现消息加密/解密

### Day 4: UI 完善
1. 设置界面功能
2. 添加联系人 UI
3. 文件传输功能
4. 消息历史加载

---

## 📝 备注

- 用户文件保存路径偏好：F 盘
- 应用数据位置：`%APPDATA%\com.nebula.chat\`
- 身份文件：`%APPDATA%\com.nebula.chat\nebula\identity.json`
- 数据库：`%APPDATA%\com.nebula.chat\nebula\messages.db`
